const Login = [13, 23, 324, 242, 86, 76]
let totalCart = 0;

for(let i = 0; i < cart.length; i += 1) {

    totalCart += cart[i];
}

console.log(totalCart);

for (const car of cart) {
    totalCart += car;
}
console.log(totalCart);
